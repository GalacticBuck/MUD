

#include "tcup.h"

//#include <locale.h>

//Jscii

/* IDEAS

	changing to use SDL
	
	TODO:
		imageloading and saving?
		window object
		colors
		bind keypad to duck tape to allow a window to listen for input from function keys
		print input binding (show what the user types for a window)
		mouse input and events per window
		timeout/interval
		exit event
		ecs
		longflag
*/

const int SCREEN_WIDTH = 640;
const int SCREEN_HEIGHT = 480;

int main (int argc, const char* argv[]){
	SDL_Window* window = NULL;
	SDL_Renderer* renderer = NULL;
	
	return 0;
}
